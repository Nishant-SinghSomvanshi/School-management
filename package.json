{
  "name": "mern-school-management-system-master",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm run start --prefix backend",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run --prefix frontend build"
  },
  "author": "Utkarsh Singh",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
